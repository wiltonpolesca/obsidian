## Goal

Create tests to DBConnection class.
This class is a facade to different database, it will use specialized class, according to database type, to connect and run queries into database.

## Constraints

- Do not create useless test, the test must be close to real world.