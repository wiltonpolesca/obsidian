# Instruções diversas

Dependências necessárias:
- pandas : `install pandas` (to update: `ip install --upgrade pandas`)
- matplotlib: `pip install matplotlib`
- numpy: `pip install numpy`
- ipykernel: `pip install ipykernel`
